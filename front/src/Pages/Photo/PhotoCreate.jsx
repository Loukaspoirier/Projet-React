import * as React from "react";
import PhotoForm from "../../Components/Photo/PhotoForm";

export default function PhotoCreate() {
  return (
    // Rendering the PhotoForm component for creating a new photo
    <PhotoForm />
  );
}
